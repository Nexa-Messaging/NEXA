import React from 'react';

import { FeaturePlaceholder } from '@/components/ui';

export default function CameraScreen() {
  return (
    <FeaturePlaceholder
      icon="camera"
      title="Camera"
      description="Capture photos and clips for disappearing stories."
      phase="Coming in a later phase"
    />
  );
}